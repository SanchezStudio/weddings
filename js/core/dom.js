import $ from "properjs-hobo";

const dom = {
  doc: $( document ),
  html: $( document.documentElement ),
  body: $( document.body ),
  header: $( ".header" ),
  nav: $( ".nav" )
};

export default dom
